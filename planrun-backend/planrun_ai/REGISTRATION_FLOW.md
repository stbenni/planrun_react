# 🔄 Процесс генерации плана при регистрации

## ✅ Да, план формируется через PlanRun AI!

### 📋 Процесс регистрации

Есть два сценария, после которых может запускаться генерация плана:

**A) Полная регистрация** (форма: режим → аккаунт → цель → профиль)

1. **RegisterScreen.jsx** — пользователь выбирает режим `ai`, `self` или `both`, заполняет цель и профиль.
2. **register_api.php** создаёт пользователя в БД `sv`, запись в `user_training_plans`.
3. Для режима **'ai' или 'both'**: проверка `isPlanRunAIAvailable()`, запуск **generate_plan_async.php** в фоне. Ответ содержит `plan_message`; фронт передаёт его на дашборд через `navigate(..., { state: { planMessage } })`.

**B) Специализация** (второй этап после минимальной регистрации)

1. После входа пользователь без `onboarding_completed` видит попап **SpecializationModal** с **RegisterScreen** (specializationOnly): шаги — режим → цель → профиль.
2. **complete_specialization_api.php** обновляет профиль в БД, при необходимости создаёт запись в `user_training_plans`.
3. Для режима **'ai' или 'both'**: так же запускается **generate_plan_async.php** в фоне. Ответ содержит `plan_message`; фронт сохраняет его в store (`setPlanGenerationMessage`) и закрывает модалку. Дашборд показывает сообщение о генерации из store (блок «План генерируется»).

**Режим 'self':** пустой календарь (недели создаются по мере добавления тренировок), генерация плана не запускается.

### 🤖 Генерация плана через PlanRun AI

**generate_plan_async.php:**
1. Вызывает `generatePlanViaPlanRunAI($userId)`

**plan_generator.php:**
1. Получает все данные пользователя из БД `sv`
2. Строит промпт через `buildTrainingPlanPrompt($userData, $goalType)`
   - Учитывает все поля: пол, возраст, опыт, цель, предпочтения
   - Адаптируется под тип цели (health/race/weight_loss/time_improvement)
3. Вызывает `callAIAPI($prompt, $userData)`

**planrun_ai_integration.php:**
1. Отправляет запрос на PlanRun AI API: `http://localhost:8000/api/v1/generate-plan`
2. Передает:
   - `user_data` - все данные пользователя
   - `goal_type` - тип цели
   - `base_prompt` - построенный промпт
   - `include_knowledge: true` - использовать RAG

**PlanRun AI API (порт 8000):**
1. RAG ищет релевантные документы в Qdrant
2. Строит финальный промпт с контекстом из базы знаний
3. Qwen3 14B генерирует план на основе промпта
4. Возвращает JSON с планом

**plan_saver.php:**
1. Сохраняет план в БД:
   - `training_plan_weeks` - недели
   - `training_plan_days` - дни недели
   - `training_day_exercises` - упражнения

2. Активирует план: `is_active = TRUE`

### ⏱️ Время генерации

- **Асинхронно:** План генерируется в фоне (3-5 минут)
- **Сообщение пользователю:** "План тренировок генерируется через PlanRun AI. Это займет 3-5 минут."

### ✅ Результат

После регистрации или специализации пользователь:
1. Остаётся авторизованным (при специализации уже был залогинен)
2. Видит на дашборде сообщение о генерации плана (`plan_message` из API — из state при полной регистрации или из store при специализации)
3. Через 3-5 минут план готов и активен
4. Может нажать «Обновить» в блоке или обновить страницу и увидеть персональный план в календаре

### 🔍 Проверка

**Логи генерации:**
- `planrun-backend/logs/plan_generation_<user_id>_<timestamp>.log` (от корня проекта)
- Или `/tmp/plan_generation_<user_id>_<timestamp>.log`

**Проверка в БД:**
```sql
SELECT * FROM sv.training_plan_weeks WHERE user_id = <user_id>;
SELECT * FROM sv.training_plan_days WHERE user_id = <user_id>;
```

---

**Да, план формируется через PlanRun AI при регистрации!** ✅
