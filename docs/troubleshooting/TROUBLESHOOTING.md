# 🔧 Решение проблемы: Не могу зайти на http://192.168.0.5:3000

## ✅ Статус сервера

**Сервер запущен и работает!**
- ✅ Процесс vite запущен (PID: 1320602)
- ✅ Порт 3000 слушает на 0.0.0.0:3000
- ✅ Локально доступен: `curl http://localhost:3000` работает
- ✅ По сети доступен: `curl http://192.168.0.5:3000` работает

## 🔍 Диагностика проблемы

### 1. Проверьте файрвол

Порт 3000 может быть заблокирован файрволом. Проверьте:

```bash
# Проверить статус файрвола
sudo ufw status

# Если порт 3000 не открыт, откройте его:
sudo ufw allow 3000/tcp
sudo ufw reload
```

### 2. Проверьте доступность с вашего компьютера

```bash
# С вашего компьютера (не с сервера):
ping 192.168.0.5
curl http://192.168.0.5:3000
```

### 3. Проверьте браузер

Откройте консоль разработчика в браузере (F12) и проверьте ошибки:
- Network tab — видите ли запросы к серверу?
- Console tab — есть ли ошибки JavaScript?

## 🚀 Быстрое решение

### Вариант 1: Открыть порт в файрволе

```bash
sudo ufw allow 3000/tcp
sudo ufw reload
```

### Вариант 2: Проверить, что сервер слушает на правильном интерфейсе

Сервер уже настроен на `0.0.0.0:3000` (все интерфейсы), это правильно.

### Вариант 3: Проверить логи сервера

Если сервер запущен в фоне, проверьте логи:
```bash
# Найти процесс vite
ps aux | grep vite

# Проверить вывод (если запущен в терминале)
```

## 📝 Текущий статус сервера

```bash
# Проверить процесс
ps aux | grep vite

# Проверить порт
netstat -tuln | grep 3000
# или
ss -tuln | grep 3000

# Проверить доступность
curl http://localhost:3000
curl http://192.168.0.5:3000
```

## 🐛 Возможные проблемы

### Проблема 1: Файрвол блокирует порт
**Решение:** `sudo ufw allow 3000/tcp`

### Проблема 2: Браузер кэширует старую версию
**Решение:** Очистите кэш браузера (Ctrl+Shift+R или Ctrl+F5)

### Проблема 3: Ошибки JavaScript в консоли
**Решение:** Откройте консоль разработчика (F12) и проверьте ошибки

### Проблема 4: Сервер не запущен
**Решение:** Из корня проекта:
```bash
./START_SERVER.sh
# или: npm run dev
```

## ✅ Что уже работает

- ✅ Сервер запущен на порту 3000
- ✅ Доступен локально (localhost:3000)
- ✅ Доступен по сети (192.168.0.5:3000) с сервера
- ✅ Нет ошибок в коде (linter проверен)

## 🎯 Следующие шаги

1. **Проверьте файрвол:** `sudo ufw status | grep 3000`
2. **Если порт закрыт:** `sudo ufw allow 3000/tcp`
3. **Попробуйте зайти снова:** http://192.168.0.5:3000
4. **Проверьте консоль браузера:** F12 → Console tab

---

**Если проблема не решена, пришлите:**
- Вывод `sudo ufw status`
- Ошибки из консоли браузера (F12 → Console)
- Результат `curl http://192.168.0.5:3000` с вашего компьютера
