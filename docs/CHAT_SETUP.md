# Настройка AI-чата PlanRun

## Требования

- **Ollama** установлен и запущен
- Модель DeepSeek (например `deepseek-r1-ru:14b`)

## Конфигурация (.env)

```env
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=deepseek-r1-ru:14b
```

## Запуск Ollama

```bash
ollama serve
ollama pull deepseek-r1-ru:14b
```

## Ошибка 500 при отправке сообщения

### 1. Ollama недоступен

Если PHP (Apache/Nginx) работает на другом хосте или в Docker, `localhost:11434` может быть недоступен.

**Решение:** укажите IP-адрес машины с Ollama в `.env`:
```env
OLLAMA_BASE_URL=http://192.168.0.6:11434
```

### 2. Модель не найдена

Проверьте доступные модели:
```bash
ollama list
```

Убедитесь, что `OLLAMA_CHAT_MODEL` совпадает с именем модели (например `deepseek-r1-ru:14b`).

### 3. Логи

Проверьте логи в `planrun-backend/logs/`:
```bash
tail -f planrun-backend/logs/error_*.log
```

### 4. Ручная проверка Ollama

```bash
curl http://localhost:11434/api/chat -d '{"model":"deepseek-r1-ru:14b","messages":[{"role":"user","content":"Привет"}],"stream":false}'
```

Если ответ приходит — Ollama работает, проблема в настройках PHP/веб-сервера.

---

## Сообщения от админа пользователям

1. Войти как администратор.
2. Открыть **Админка** (в меню профиля).
3. Вкладка **Пользователи** → нажать кнопку 💬 у нужного пользователя.
4. Ввести текст и нажать **Отправить**.

Пользователь увидит сообщение во вкладке чата **«От администрации»**.

### SSE (Server-Sent Events) — real-time уведомления

Для real-time счётчика непрочитанных используется SSE. Endpoint: `/api/chat_sse.php`.

**Nginx** — для долгих соединений добавьте в `location` для API:
```nginx
proxy_read_timeout 3600s;
proxy_buffering off;
proxy_cache off;
```

**PHP** — `max_execution_time` для SSE должен быть 0 (скрипт использует `set_time_limit(0)`).

### Сообщения не отображаются

1. Убедитесь, что смотрите чат под **учётной записью получателя** (не админа).
2. Админ отправляет сообщение **конкретному пользователю** — проверьте ID в админке.
3. Перейдите во вкладку «От администрации» и нажмите кнопку ↻ (Обновить).
4. В DevTools (Network) проверьте ответ `chat_get_messages?type=admin` — есть ли там массив `messages`.
5. Проверить БД: `php planrun-backend/scripts/check_chat_debug.php 1` (1 — ваш user_id) — есть ли сообщения в `chat_messages`.
