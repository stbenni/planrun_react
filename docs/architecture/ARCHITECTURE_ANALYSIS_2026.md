# 📊 Анализ архитектуры проекта PlanRun (2026)

**Дата анализа:** 25 января 2026  
**Версия проекта:** Текущая  
**Аналитик:** AI Assistant

---

## 📋 Содержание

1. [Текущая архитектура](#текущая-архитектура)
2. [Сильные стороны](#сильные-стороны)
3. [Области для улучшения](#области-для-улучшения)
4. [Рекомендации по архитектуре](#рекомендации-по-архитектуре)
5. [Современные альтернативы 2026](#современные-альтернативы-2026)
6. [План миграции](#план-миграции)

---

## 🏗️ Текущая архитектура

### Технологический стек

**Фронтенд:**
- React 18.2.0 с Vite 5.4.2
- React Router DOM 6.20.0
- Capacitor 8.0.1 (для мобильных приложений)
- CSS модули и кастомные стили

**Бэкенд:**
- PHP (версия не указана, рекомендуется PHP 8.3+)
- MySQL (mysqli)
- Монолитный API (`api.php` - 2760 строк)
- PHP сессии для аутентификации

**Инфраструктура:**
- Файловый кеш (с поддержкой Redis/Memcached)
- Кастомная система логирования
- Rate Limiting
- Интеграция с локальной LLM (Qwen3 через FastAPI)

**База данных:**
- MySQL (база `running_calendar`)
- Использование mysqli (процедурный стиль)

---

## ✅ Сильные стороны

### 1. **Современный фронтенд**
- ✅ Использование React 18 с современными хуками
- ✅ Lazy loading компонентов для оптимизации
- ✅ Vite для быстрой разработки
- ✅ Поддержка мобильных приложений через Capacitor

### 2. **Хорошая инфраструктура**
- ✅ Структурированная система логирования (`Logger.php`)
- ✅ Rate Limiting для защиты API
- ✅ Обработка ошибок (`ErrorHandler`)
- ✅ Поддержка кеширования (Redis/Memcached/File)

### 3. **AI интеграция**
- ✅ Локальная LLM (Qwen3) для генерации планов
- ✅ RAG система для контекстной генерации
- ✅ Асинхронная генерация планов

### 4. **Безопасность**
- ✅ Использование `password_verify()` для хеширования паролей
- ✅ Prepared statements для защиты от SQL инъекций
- ✅ CORS настройки
- ✅ Rate Limiting

---

## ⚠️ Области для улучшения

### 🔴 Критические проблемы

#### 1. **Монолитный API файл**
- **Проблема:** `api.php` содержит 2760 строк кода
- **Риски:**
  - Сложность поддержки и тестирования
  - Высокий риск конфликтов при работе в команде
  - Нарушение принципа единственной ответственности
- **Влияние:** Высокое

#### 2. **Процедурный стиль PHP**
- **Проблема:** Использование mysqli в процедурном стиле вместо ООП
- **Риски:**
  - Сложность тестирования
  - Дублирование кода
  - Отсутствие абстракции данных
- **Влияние:** Среднее-Высокое

#### 3. **Отсутствие фреймворка**
- **Проблема:** Чистый PHP без использования современных фреймворков
- **Риски:**
  - Отсутствие стандартизации
  - Нет встроенной валидации, роутинга, DI
  - Сложность масштабирования
- **Влияние:** Высокое

#### 4. **База данных: MySQL вместо PostgreSQL**
- **Проблема:** MySQL имеет ограничения в производительности и функциональности
- **Риски:**
  - Медленнее на сложных запросах (до 9-13x разница)
  - Ограниченная поддержка JSON операций
  - Проблемы с конкурентностью
- **Влияние:** Среднее

#### 5. **Отсутствие тестирования**
- **Проблема:** Нет unit/integration тестов
- **Риски:**
  - Высокий риск регрессий
  - Сложность рефакторинга
  - Нет уверенности в работоспособности
- **Влияние:** Высокое

### 🟡 Важные улучшения

#### 6. **Управление состоянием на фронтенде**
- **Проблема:** Нет централизованного state management
- **Риски:**
  - Prop drilling
  - Дублирование состояния
  - Сложность синхронизации данных
- **Влияние:** Среднее

#### 7. **Версионирование API**
- **Проблема:** Нет версионирования API
- **Риски:**
  - Сложность обновлений без breaking changes
  - Проблемы с мобильными приложениями
- **Влияние:** Среднее

#### 8. **Отсутствие CI/CD**
- **Проблема:** Нет автоматизации деплоя и тестирования
- **Риски:**
  - Ручные ошибки при деплое
  - Нет автоматического тестирования
- **Влияние:** Среднее

#### 9. **Конфигурация в коде**
- **Проблема:** Пароли БД и настройки в коде
- **Риски:**
  - Проблемы безопасности
  - Сложность управления окружениями
- **Влияние:** Высокое (безопасность)

#### 10. **Отсутствие TypeScript**
- **Проблема:** JavaScript без типизации
- **Риски:**
  - Ошибки времени выполнения
  - Сложность рефакторинга
- **Влияние:** Среднее

---

## 🎯 Рекомендации по архитектуре

### 1. **Миграция на современный PHP фреймворк**

#### Вариант A: Laravel (Рекомендуется)
**Преимущества:**
- ✅ Огромная экосистема и сообщество
- ✅ Eloquent ORM (более удобный чем чистый mysqli)
- ✅ Встроенная валидация, роутинг, миграции
- ✅ Отличная документация
- ✅ Laravel Sanctum для API аутентификации
- ✅ Laravel Queue для асинхронных задач
- ✅ Laravel Horizon для мониторинга очередей

**Структура:**
```
app/
├── Http/
│   ├── Controllers/
│   │   ├── Api/
│   │   │   ├── TrainingPlanController.php
│   │   │   ├── WorkoutController.php
│   │   │   └── StatsController.php
│   │   └── AuthController.php
│   ├── Requests/          # Валидация
│   └── Resources/         # API Resources
├── Models/
│   ├── User.php
│   ├── TrainingPlan.php
│   └── Workout.php
├── Services/
│   ├── PlanGeneratorService.php
│   └── AIService.php
└── Jobs/
    └── GeneratePlanJob.php
```

#### Вариант B: Symfony
**Преимущества:**
- ✅ Более гибкий и модульный
- ✅ Лучше для enterprise приложений
- ✅ Doctrine ORM (мощнее Eloquent)
- ✅ Компонентный подход

**Когда выбирать:** Если нужна максимальная гибкость и модульность

### 2. **Рефакторинг API на RESTful архитектуру**

**Текущая структура:**
```
GET /api.php?action=load
GET /api.php?action=get_day&date=2026-01-25
POST /api.php?action=save_result
```

**Рекомендуемая структура:**
```
GET    /api/v1/training-plans          # Список планов
GET    /api/v1/training-plans/{id}     # Конкретный план
POST   /api/v1/training-plans           # Создать план
PUT    /api/v1/training-plans/{id}      # Обновить план
DELETE /api/v1/training-plans/{id}      # Удалить план

GET    /api/v1/workouts?date=2026-01-25 # Тренировки за дату
POST   /api/v1/workouts                 # Сохранить результат
GET    /api/v1/workouts/{id}            # Конкретная тренировка

GET    /api/v1/stats                    # Статистика
POST   /api/v1/adaptation               # Запустить адаптацию
```

**Преимущества:**
- ✅ Стандартизация
- ✅ Лучшая кешируемость
- ✅ Проще для фронтенда
- ✅ Поддержка версионирования

### 3. **Миграция на PostgreSQL**

**Преимущества PostgreSQL 18:**
- ✅ **Производительность:** До 13x быстрее на сложных запросах
- ✅ **JSON поддержка:** Нативная работа с JSON (важно для AI данных)
- ✅ **Конкурентность:** MVCC для лучшей параллельной работы
- ✅ **Расширяемость:** JSONB индексы, полнотекстовый поиск
- ✅ **Надежность:** Лучшая ACID совместимость

**Миграция:**
```sql
-- Использование pgloader для миграции
pgloader mysql://user:pass@localhost/running_calendar \
         postgresql://user:pass@localhost/planrun_db
```

### 4. **Внедрение TypeScript на фронтенде**

**Преимущества:**
- ✅ Типобезопасность
- ✅ Лучший автокомплит
- ✅ Раннее обнаружение ошибок
- ✅ Улучшенная документация кода

**Миграция:**
```bash
# Установка TypeScript
npm install -D typescript @types/react @types/react-dom

# Переименование файлов .jsx -> .tsx
# Постепенная миграция с allowJs: true
```

### 5. **State Management на фронтенде**

#### Вариант A: Zustand (Рекомендуется для небольших-средних приложений)
```typescript
// store/useAuthStore.ts
import create from 'zustand'

interface AuthState {
  user: User | null
  login: (username: string, password: string) => Promise<void>
  logout: () => void
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  login: async (username, password) => {
    const user = await api.login(username, password)
    set({ user })
  },
  logout: () => set({ user: null })
}))
```

#### Вариант B: React Query + Context API
- React Query для серверного состояния
- Context API для глобального UI состояния

**Преимущества:**
- ✅ Автоматическое кеширование
- ✅ Оптимистичные обновления
- ✅ Retry логика
- ✅ Меньше boilerplate чем Redux

### 6. **Внедрение тестирования**

#### PHP (PHPUnit)
```php
// tests/Unit/Services/PlanGeneratorServiceTest.php
class PlanGeneratorServiceTest extends TestCase
{
    public function test_generates_plan_for_beginner()
    {
        $service = new PlanGeneratorService();
        $plan = $service->generate([
            'level' => 'beginner',
            'goal' => 'marathon'
        ]);
        
        $this->assertInstanceOf(TrainingPlan::class, $plan);
        $this->assertEquals(16, $plan->weeks->count());
    }
}
```

#### Frontend (Vitest + React Testing Library)
```typescript
// src/components/Calendar/__tests__/Calendar.test.tsx
import { render, screen } from '@testing-library/react'
import { Calendar } from '../Calendar'

test('renders calendar with current month', () => {
  render(<Calendar />)
  expect(screen.getByText('January 2026')).toBeInTheDocument()
})
```

### 7. **Улучшение безопасности**

#### Аутентификация: JWT вместо PHP сессий
**Преимущества:**
- ✅ Stateless (лучше для API)
- ✅ Работает с мобильными приложениями
- ✅ Масштабируемость
- ✅ Refresh tokens

```php
// Laravel Sanctum example
use Laravel\Sanctum\HasApiTokens;

class User extends Model
{
    use HasApiTokens;
    
    public function createToken($name)
    {
        return $this->tokens()->create([
            'name' => $name,
            'token' => hash('sha256', $plainTextToken),
            'abilities' => ['*'],
        ]);
    }
}
```

#### Биометрическая аутентификация для мобильных приложений
**Преимущества:**
- ✅ Удобство для пользователей (отпечаток пальца, Face ID)
- ✅ Повышенная безопасность
- ✅ Быстрый вход без ввода пароля
- ✅ Поддержка всех типов биометрии (Fingerprint, Face, Iris)

**Реализация:**
- Использование `@capawesome-team/capacitor-biometrics` для Capacitor 8.x
- Локальное хранение зашифрованных токенов в Keychain/Keystore
- Биометрия как дополнительный фактор после первичной авторизации
- Fallback на PIN/пароль при недоступности биометрии

**Архитектура:**
1. Первый вход: логин/пароль → получение JWT токена
2. Сохранение токена в защищенном хранилище (Keychain/Keystore)
3. Последующие входы: биометрия → получение токена из хранилища
4. Обновление токена через API при необходимости

#### Конфигурация через .env
```env
# .env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=running_calendar
DB_USERNAME=root
DB_PASSWORD=${DB_PASSWORD}  # Из secrets manager

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null

JWT_SECRET=${JWT_SECRET}
```

### 8. **CI/CD Pipeline**

```yaml
# .github/workflows/ci.yml
name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
      - name: Install dependencies
        run: composer install
      - name: Run tests
        run: phpunit

  test-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm test
      - name: Build
        run: npm run build

  deploy:
    needs: [test-backend, test-frontend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy to production
        run: |
          # Deployment script
```

### 9. **Мониторинг и логирование**

#### Внедрение структурированного логирования
- **Laravel:** Использовать Monolog с каналами
- **Frontend:** Sentry для отслеживания ошибок

```php
// config/logging.php
'channels' => [
    'stack' => [
        'driver' => 'stack',
        'channels' => ['daily', 'slack'],
    ],
    'daily' => [
        'driver' => 'daily',
        'path' => storage_path('logs/laravel.log'),
        'level' => 'debug',
        'days' => 14,
    ],
],
```

### 10. **Оптимизация производительности**

#### Backend
- ✅ Использовать Redis для кеширования (вместо файлового)
- ✅ Query optimization с индексами
- ✅ Eager loading для избежания N+1 проблем
- ✅ Очереди для тяжелых операций (генерация планов)

#### Frontend
- ✅ Code splitting по маршрутам
- ✅ Lazy loading изображений
- ✅ Service Worker для кеширования
- ✅ React.memo для оптимизации рендеринга

---

## 🚀 Современные альтернативы 2026

### 1. **Backend: Laravel vs Symfony vs Node.js**

| Критерий | Laravel | Symfony | Node.js (NestJS) |
|----------|---------|---------|------------------|
| Простота | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| Производительность | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Экосистема | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Для API | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Кривая обучения | Низкая | Средняя | Средняя |

**Рекомендация:** Laravel для быстрой разработки, Symfony для enterprise, NestJS если команда знает TypeScript

### 2. **Frontend: React vs Vue vs Svelte**

| Критерий | React | Vue 3 | Svelte |
|----------|-------|-------|--------|
| Популярность | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| Производительность | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Размер бандла | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Экосистема | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

**Рекомендация:** Остаться на React (уже используется, отличная экосистема)

### 3. **База данных: MySQL vs PostgreSQL vs MongoDB**

| Критерий | MySQL | PostgreSQL | MongoDB |
|----------|-------|------------|---------|
| Производительность | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| JSON поддержка | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| ACID | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| Для вашего случая | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

**Рекомендация:** PostgreSQL 18 (лучшая производительность, JSON поддержка для AI данных)

### 4. **State Management: Redux vs Zustand vs Jotai**

| Критерий | Redux Toolkit | Zustand | Jotai |
|----------|---------------|---------|-------|
| Простота | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Размер | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Производительность | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**Рекомендация:** Zustand для простоты или React Query для серверного состояния

---

## 📅 План миграции

### Фаза 1: Подготовка (1-2 недели)
- [ ] Настройка CI/CD
- [ ] Внедрение тестирования (начать с критичных частей)
- [ ] Миграция конфигурации на .env
- [ ] Настройка мониторинга

### Фаза 2: Backend рефакторинг (4-6 недель)
- [ ] Установка Laravel
- [ ] Миграция моделей и БД схемы
- [ ] Рефакторинг API на контроллеры
- [ ] Внедрение валидации
- [ ] Миграция на JWT аутентификацию
- [ ] Параллельная работа старого и нового API

### Фаза 3: Frontend улучшения (2-3 недели)
- [ ] Внедрение TypeScript
- [ ] Добавление state management (Zustand/React Query)
- [ ] Оптимизация производительности
- [ ] Улучшение UX

### Фаза 4: База данных (2-3 недели)
- [ ] Миграция на PostgreSQL
- [ ] Оптимизация запросов
- [ ] Настройка репликации (если нужно)

### Фаза 5: Финализация (1-2 недели)
- [ ] Полное тестирование
- [ ] Документация API
- [ ] Обучение команды
- [ ] Постепенный rollout

**Общее время:** 10-16 недель

---

## 💰 Оценка затрат

### Время разработки
- Backend рефакторинг: 200-300 часов
- Frontend улучшения: 80-120 часов
- Тестирование: 100-150 часов
- Миграция БД: 40-60 часов
- **Итого:** 420-630 часов

### Инфраструктура
- PostgreSQL сервер: +20% к текущим затратам
- Redis: Минимальные затраты (можно на том же сервере)
- CI/CD (GitHub Actions): Бесплатно до определенного лимита
- Мониторинг (Sentry): ~$26/месяц для старта

---

## 🎯 Приоритеты

### Высокий приоритет (сделать в первую очередь)
1. ✅ Миграция конфигурации на .env
2. ✅ Внедрение базового тестирования
3. ✅ Рефакторинг монолитного API
4. ✅ Внедрение TypeScript

### Средний приоритет
5. ✅ Миграция на Laravel/Symfony
6. ✅ State management на фронтенде
7. ✅ CI/CD pipeline
8. ✅ Миграция на PostgreSQL

### Низкий приоритет (можно отложить)
9. ⏳ GraphQL API (если нужна гибкость)
10. ⏳ Microservices (если масштабирование критично)

---

## 📚 Полезные ресурсы

### Документация
- [Laravel Documentation](https://laravel.com/docs)
- [React Best Practices 2026](https://react.dev/learn)
- [PostgreSQL 18 Documentation](https://www.postgresql.org/docs/18/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)

### Инструменты
- [Laravel Sanctum](https://laravel.com/docs/sanctum) - API аутентификация
- [React Query](https://tanstack.com/query) - Серверное состояние
- [Zustand](https://github.com/pmndrs/zustand) - State management
- [Vitest](https://vitest.dev/) - Тестирование фронтенда

---

## ✅ Заключение

Проект имеет хорошую основу, но нуждается в модернизации архитектуры для:
- **Масштабируемости:** Текущая монолитная структура ограничивает рост
- **Поддерживаемости:** Большие файлы сложно поддерживать
- **Производительности:** PostgreSQL и оптимизации дадут значительный прирост
- **Безопасности:** JWT и правильная конфигурация улучшат безопасность
- **Качества кода:** Тестирование и TypeScript снизят количество багов

**Рекомендуемый путь:** Постепенная миграция с сохранением работоспособности системы.

---

**Вопросы?** Создайте issue или свяжитесь с командой разработки.
