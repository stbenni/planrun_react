# 📦 Руководство по переносу приложения

## ✅ Что НЕ нужно менять (уже портабельно)

1. **PHP файлы** - все используют `__DIR__` (относительные пути)
2. **React код** - использует относительные пути `/api`
3. **ApiClient.js** - автоматически определяет baseUrl
4. **CORS** - динамическая проверка origin

## 🔧 Что нужно изменить при переносе

### 1. Apache конфигурация

Файлы:
- `/etc/apache2/sites-available/s-vladimirov.conf`
- `/etc/apache2/sites-available/s-vladimirov-le-ssl.conf`

Изменить:
- `ServerName` - новый домен
- `DocumentRoot` - новый путь к файлам
- `ProxyPass` - порт React приложения (если изменился)
- SSL сертификаты (если новый домен)

### 2. Systemd service

Файл: `/etc/systemd/system/s-vladimirov-react.service`

Изменить:
- `WorkingDirectory` - новый путь
- `ExecStart` - путь к скрипту запуска

### 3. Vite конфигурация (опционально)

Файл: `vite.config.js`

Изменить:
- `port` - если нужен другой порт
- `host` - если нужен другой хост

### 4. Комментарии в cron задачах (опционально)

Файлы:
- `planrun-backend/deepseek/weekly_adaptation.php` (строка 6)
- `planrun-backend/deepseek/test_normalizer.php` (строка 5)

Изменить пути в комментариях для документации.

### 5. База данных

Файл: `planrun-backend/db_config.php`

Изменить:
- `DB_HOST` - если БД на другом сервере
- `DB_NAME` - если другая БД
- `DB_USER` / `DB_PASS` - учетные данные

## 🚀 Быстрый перенос

1. Скопировать всю папку `/var/www/s-vladimirov.ru` в новое место
2. Обновить Apache конфигурацию
3. Обновить systemd service
4. Перезапустить сервисы:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl restart apache2
   sudo systemctl restart s-vladimirov-react.service
   ```

## ✅ Проверка после переноса

1. Проверить доступность React приложения
2. Проверить работу API (`/api/api_wrapper.php?action=check_auth`)
3. Проверить логин/логаут
4. Проверить работу с БД

## 📝 Примечания

- Все пути в PHP используют `__DIR__` - автоматически адаптируются
- React использует относительные пути `/api` - работает на любом домене
- CORS настроен динамически - принимает запросы с любого домена
- Единственная связь между приложениями - общая БД (`sv`)
