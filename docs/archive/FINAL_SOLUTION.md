# 🎯 Финальное решение проблемы

## Проблема
npm install не завершается или не создает node_modules. Возможные причины:
- Зависание процесса
- Проблемы с правами
- Несовместимость версий

## ✅ Решение: Обновить Node.js через nvm

### Шаг 1: Установить nvm
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
```

### Шаг 2: Установить Node.js 20
```bash
nvm install 20
nvm use 20
node --version  # Должно быть v20.x.x
```

### Шаг 3: Установить зависимости
```bash
cd /var/www/planrun/react/web
rm -rf node_modules package-lock.json
npm install
```

### Шаг 4: Запустить dev сервер
```bash
npm run dev
```

## Альтернатива: Сборка на другой машине

Если обновление Node.js не помогает, можно:
1. Собрать на локальной машине с Node.js 20+
2. Загрузить папку `dist/` на сервер
3. Настроить Apache для обслуживания статических файлов

## Быстрая проверка

Попробуйте:
```bash
# Проверить версию Node
node --version

# Если меньше 20, обновить через nvm
nvm install 20
nvm use 20

# Затем установить зависимости
cd /var/www/planrun/react/web
npm install
```

---

**Рекомендация:** Обновите Node.js до версии 20+ через nvm - это решит все проблемы с совместимостью.
