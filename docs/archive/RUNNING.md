# 🎉 React приложение ЗАПУЩЕНО!

## ✅ Статус

- **Vite сервер:** ✅ Работает
- **Порт:** 3000
- **URL:** http://localhost:3000
- **Network URL:** http://192.168.0.5:3000

## 🚀 Как зайти

### С сервера
```bash
curl http://localhost:3000
```

### С вашего компьютера через SSH туннель
```bash
ssh -L 3000:localhost:3000 st_benni@planrun.ru
```
Затем откройте в браузере: `http://localhost:3000`

### Прямой доступ (если открыт порт)
```
http://planrun.ru:3000
```
или
```
http://192.168.0.5:3000
```

## 📝 Управление сервером

### Проверить статус
```bash
ps aux | grep vite
curl http://localhost:3000
```

### Остановить
```bash
pkill -f vite
```

### Перезапустить
```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm use 20
cd /var/www/planrun/react/web
npm run dev
```

### Логи
```bash
tail -f /tmp/vite_final.log
```

## ⚙️ Важно

- Используется **Node.js 20.20.0** через nvm
- При новом терминале загрузите nvm:
  ```bash
  export NVM_DIR="$HOME/.nvm"
  [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
  nvm use 20
  ```

## 🎯 Что дальше

1. Откройте приложение в браузере
2. Войдите с существующими учетными данными
3. Проверьте работу календаря
4. Тестируйте функциональность

---

**Приложение готово к использованию!** 🚀
