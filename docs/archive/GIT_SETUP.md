# 🔄 Настройка Git репозитория для резервного копирования

## ✅ Что уже сделано

- ✅ Git репозиторий инициализирован
- ✅ Создан `.gitignore` (исключены пароли, node_modules, кэши)
- ✅ Первый коммит создан (173 файла, 31378 строк кода)
- ✅ Конфиги с паролями НЕ добавлены в репозиторий

## 📋 Следующие шаги

### Вариант 1: GitHub (рекомендуется)

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Название: `planrun-react` (или любое другое)
   - Выберите **Private** (для безопасности)
   - НЕ создавайте README, .gitignore или лицензию (они уже есть)

2. **Подключите локальный репозиторий к GitHub:**
   ```bash
   cd /var/www/s-vladimirov.ru
   git remote add origin https://github.com/ВАШ_USERNAME/planrun-react.git
   git branch -M main
   git push -u origin main
   ```

3. **Если нужна авторизация через SSH:**
   ```bash
   # Создайте SSH ключ (если еще нет)
   ssh-keygen -t ed25519 -C "your_email@example.com"
   
   # Скопируйте публичный ключ
   cat ~/.ssh/id_ed25519.pub
   
   # Добавьте его в GitHub: Settings → SSH and GPG keys → New SSH key
   
   # Используйте SSH URL вместо HTTPS
   git remote set-url origin git@github.com:ВАШ_USERNAME/planrun-react.git
   git push -u origin main
   ```

### Вариант 2: GitLab

1. **Создайте проект на GitLab:**
   - Перейдите на https://gitlab.com/projects/new
   - Название: `planrun-react`
   - Выберите **Private**

2. **Подключите репозиторий:**
   ```bash
   cd /var/www/s-vladimirov.ru
   git remote add origin https://gitlab.com/ВАШ_USERNAME/planrun-react.git
   git branch -M main
   git push -u origin main
   ```

### Вариант 3: Другой Git сервер

```bash
cd /var/www/s-vladimirov.ru
git remote add origin URL_ВАШЕГО_РЕПОЗИТОРИЯ
git branch -M main
git push -u origin main
```

## 🔄 Регулярное резервное копирование

После настройки удаленного репозитория, для создания резервной копии:

```bash
cd /var/www/s-vladimirov.ru
git add .
git commit -m "Backup: $(date '+%Y-%m-%d %H:%M:%S')"
git push
```

## 🤖 Автоматическое резервное копирование (cron)

Создайте скрипт для автоматического бэкапа:

```bash
# Создайте файл
nano /var/www/s-vladimirov.ru/backup.sh
```

Содержимое:
```bash
#!/bin/bash
cd /var/www/s-vladimirov.ru
git add .
git commit -m "Auto backup: $(date '+%Y-%m-%d %H:%M:%S')" || exit 0
git push || exit 0
```

Сделайте исполняемым:
```bash
chmod +x /var/www/s-vladimirov.ru/backup.sh
```

Добавьте в crontab (резервная копия каждый день в 3:00):
```bash
crontab -e
# Добавьте строку:
0 3 * * * /var/www/s-vladimirov.ru/backup.sh
```

## 🔒 Безопасность

⚠️ **Важно:** Файлы с паролями (`db_config.php`, `local_llm_config.php`) НЕ добавлены в репозиторий благодаря `.gitignore`.

Если нужно сохранить конфиги отдельно:
1. Создайте файлы-примеры: `db_config.php.example`
2. Или используйте переменные окружения
3. Или храните конфиги в отдельном приватном репозитории

## 📊 Статистика репозитория

- **Файлов:** 173
- **Строк кода:** 31,378
- **Размер:** ~2-3 MB (без node_modules)

## 🆘 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть историю
git log --oneline

# Посмотреть удаленные репозитории
git remote -v

# Изменить URL удаленного репозитория
git remote set-url origin НОВЫЙ_URL

# Получить изменения с сервера
git pull

# Отправить изменения на сервер
git push
```
