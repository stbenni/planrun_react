# 🔄 Восстановление из резервной копии

## ✅ Резервная копия создана

**Репозиторий:** `git@github.com:stbenni/planrun_react.git`

Все файлы сохранены в GitHub и доступны для восстановления в любой момент.

## 🚨 Восстановление на текущем сервере

### Вариант 1: Если папка повреждена, но Git репозиторий цел

```bash
cd /var/www/s-vladimirov.ru
git fetch origin
git reset --hard origin/main
```

### Вариант 2: Если папка полностью удалена

```bash
# Клонируем репозиторий заново
cd /var/www
git clone git@github.com:stbenni/planrun_react.git s-vladimirov.ru-restored

# Или если хотите в ту же папку
rm -rf /var/www/s-vladimirov.ru
git clone git@github.com:stbenni/planrun_react.git /var/www/s-vladimirov.ru
```

### Вариант 3: Восстановление конкретного файла

```bash
cd /var/www/s-vladimirov.ru
git checkout origin/main -- путь/к/файлу
```

### Вариант 4: Откат к конкретному коммиту

```bash
cd /var/www/s-vladimirov.ru
# Посмотреть историю
git log --oneline

# Откатиться к нужному коммиту
git reset --hard <hash_коммита>

# Или откатиться на N коммитов назад
git reset --hard HEAD~1  # на 1 коммит назад
```

## 📋 Что НЕ сохранено в резервной копии

⚠️ **Важно:** Эти файлы нужно восстановить вручную:

1. **`planrun-backend/db_config.php`** - конфигурация БД с паролями
   - Создайте файл заново с правильными данными подключения

2. **`planrun-backend/deepseek/local_llm_config.php`** - конфигурация AI (если используется)
   - Используйте `local_llm_config.example.php` как шаблон

3. **`node_modules/`** - зависимости Node.js
   - Восстановите командой: `npm install`

4. **`dist/`** - собранные файлы
   - Пересоберите: `npm run build`

5. **Кэши и логи:**
   - `planrun-backend/cache/`
   - `planrun-backend/logs/`

## 🔧 Полное восстановление после катастрофы

```bash
# 1. Клонируем репозиторий
cd /var/www
git clone git@github.com:stbenni/planrun_react.git s-vladimirov.ru

# 2. Восстанавливаем зависимости
cd s-vladimirov.ru
npm install

# 3. Восстанавливаем конфиги (создайте вручную)
# - planrun-backend/db_config.php
# - planrun-backend/deepseek/local_llm_config.php (если нужно)

# 4. Собираем проект (если нужно)
npm run build

# 5. Настраиваем права доступа
chown -R www-data:www-data /var/www/s-vladimirov.ru
chmod -R 755 /var/www/s-vladimirov.ru
```

## 📊 Проверка целостности резервной копии

```bash
# Проверить, что репозиторий доступен
git ls-remote git@github.com:stbenni/planrun_react.git

# Посмотреть все коммиты
git log --oneline --all

# Посмотреть размер репозитория
du -sh .git
```

## 🔄 Регулярное обновление резервной копии

Чтобы всегда иметь актуальную резервную копию:

```bash
cd /var/www/s-vladimirov.ru
git add .
git commit -m "Backup: $(date '+%Y-%m-%d %H:%M:%S')"
git push
```

## 🆘 Если что-то пошло не так

1. **Проверьте доступ к GitHub:**
   ```bash
   ssh -T git@github.com
   ```

2. **Проверьте статус репозитория:**
   ```bash
   cd /var/www/s-vladimirov.ru
   git status
   git remote -v
   ```

3. **Если нужно восстановить удаленные файлы:**
   ```bash
   git checkout HEAD -- .
   ```

4. **Если нужно отменить локальные изменения:**
   ```bash
   git reset --hard origin/main
   ```

## ✅ Резюме

- ✅ **Все исходники сохранены** в GitHub
- ✅ **Можно восстановить** в любой момент
- ⚠️ **Конфиги с паролями** нужно восстановить вручную
- ⚠️ **Зависимости** нужно установить заново (`npm install`)

**Резервная копия работает!** 🎉
