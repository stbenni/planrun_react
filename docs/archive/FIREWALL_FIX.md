# 🔥 Настройка файрвола для порта 3000

## Проблема
Нельзя зайти по http://192.168.0.5:3000/ - файрвол блокирует порт.

## Решение

### Вариант 1: UFW (Ubuntu)
```bash
sudo ufw allow 3000/tcp
sudo ufw reload
sudo ufw status
```

### Вариант 2: Firewalld (CentOS/RHEL)
```bash
sudo firewall-cmd --permanent --add-port=3000/tcp
sudo firewall-cmd --reload
sudo firewall-cmd --list-ports
```

### Вариант 3: iptables
```bash
sudo iptables -A INPUT -p tcp --dport 3000 -j ACCEPT
sudo iptables-save
```

## Проверка

После настройки файрвола проверьте:
```bash
# С сервера
curl http://192.168.0.5:3000

# С другого компьютера в той же сети
curl http://192.168.0.5:3000
```

## Важно

- Сервер уже настроен на `host: '0.0.0.0'` в vite.config.js
- Это означает что он слушает на всех интерфейсах
- Осталось только открыть порт в файрволе
