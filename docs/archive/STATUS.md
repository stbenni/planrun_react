# ✅ Статус React приложения

## Установка завершена!

- ✅ Node.js установлен (v18.19.1)
- ✅ npm установлен (v9.2.0)
- ✅ Зависимости установлены (922 пакета)
- ✅ Dev сервер запущен

## 🚀 Как зайти

### Вариант 1: Локально на сервере
```
http://localhost:3000
```

### Вариант 2: Через SSH туннель (с вашего компьютера)
```bash
ssh -L 3000:localhost:3000 st_benni@planrun.ru
```
Затем откройте в браузере: `http://localhost:3000`

### Вариант 3: Прямой доступ (если настроен firewall)
```
http://planrun.ru:3000
```
(Нужно открыть порт 3000 в firewall)

## 📝 Команды

### Остановить сервер
Нажмите `Ctrl+C` в терминале где запущен сервер

### Перезапустить
```bash
cd /var/www/planrun/react/web
npm run dev
```

### Собрать production версию
```bash
npm run build
```
Создаст папку `dist/` со статическими файлами

## ⚠️ Важно

- Dev сервер работает только пока запущен процесс
- Для постоянного доступа нужно собрать production версию и настроить Apache
- API запросы идут через proxy на `http://localhost/planrun`

## 🐛 Если не работает

1. Проверьте что сервер запущен: `ps aux | grep vite`
2. Проверьте порт: `netstat -tuln | grep 3000`
3. Проверьте логи в терминале где запущен сервер
