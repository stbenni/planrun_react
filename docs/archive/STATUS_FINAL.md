# ✅ Сервер запущен и работает!

## Статус

- ✅ **Порт 3000 открыт в файрволе** (UFW)
- ✅ **Сервер запущен** на 0.0.0.0:3000
- ✅ **Доступен по сети** http://192.168.0.5:3000
- ✅ **Локально** http://localhost:3000

## 🚀 Как зайти

### С любого компьютера в сети:
```
http://192.168.0.5:3000
```

### С сервера:
```
http://localhost:3000
```

## 📝 Управление

### Проверить статус:
```bash
ps aux | grep vite
netstat -tuln | grep 3000
curl http://192.168.0.5:3000
```

### Остановить:
```bash
pkill -f vite
```

### Запустить заново:
```bash
cd /var/www/planrun/react/web
./START_SERVER.sh
```

Или вручную:
```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm use 20
cd /var/www/planrun/react/web
npm run dev
```

## ⚡ Производительность

Если работает медленно, возможные причины:
1. Первая загрузка - компиляция React кода
2. Много процессов vite - остановите лишние: `pkill -f vite`
3. Проблемы с сетью - проверьте ping до сервера

## 🐛 Отладка

Если не работает:
1. Проверьте что сервер запущен: `ps aux | grep vite`
2. Проверьте порт: `netstat -tuln | grep 3000`
3. Проверьте файрвол: `sudo ufw status | grep 3000`
4. Проверьте логи: `tail -f /tmp/vite_server.log`

---

**Всё готово! Откройте http://192.168.0.5:3000 в браузере!** 🎉
