# 🔧 Исправление CORS и редиректов

## Проблема
Запросы шли на `http://192.168.0.5:3000/login.php`, но Apache делал редирект на `https://to-baikal.ru/planrun/login.php`, что вызывало CORS ошибки.

## Решение

### 1. Обновлен `vite.config.js`
- Изменен `target` с `http://localhost/planrun` на `http://127.0.0.1/planrun`
- Установлен `changeOrigin: false` чтобы избежать редиректов
- Добавлен `configure` для установки правильного Host header

### 2. Добавлены CORS заголовки в `login.php`
- Добавлена поддержка CORS для AJAX запросов
- Обработка preflight OPTIONS запросов

## Как проверить

1. Обновите страницу в браузере (Ctrl+F5)
2. Попробуйте залогиниться
3. Проверьте Network tab в браузере - запросы должны идти через прокси без редиректов

## Отладка

Если все еще не работает:

1. Проверьте что сервер запущен:
   ```bash
   ps aux | grep vite
   netstat -tuln | grep 3000
   ```

2. Проверьте логи:
   ```bash
   tail -f /tmp/vite_server.log
   ```

3. Проверьте что прокси работает:
   ```bash
   curl -v http://127.0.0.1:3000/login.php
   ```
