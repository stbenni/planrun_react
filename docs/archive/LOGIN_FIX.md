# 🔧 Исправление проблемы с логином

## Проблема
Не удавалось залогиниться в React приложении.

## Причина
`login.php` возвращал HTML редирект вместо JSON для AJAX запросов.

## Решение

### 1. Обновлен `login.php`
Добавлена проверка на AJAX запросы:
- Если запрос AJAX (заголовок `X-Requested-With: XMLHttpRequest`) или запрашивается JSON (`Accept: application/json`)
- Возвращается JSON ответ: `{"success": true}` или `{"success": false, "error": "..."}`
- Иначе возвращается HTML редирект (для обычных форм)

### 2. Обновлен `ApiClient.js`
- Добавлены заголовки `X-Requested-With` и `Accept: application/json` в запрос логина
- Улучшена обработка ответа (проверка Content-Type)
- Улучшена обработка ошибок в `getCurrentUser()`

## Как проверить

1. Откройте React приложение: http://192.168.0.5:3000
2. Попробуйте залогиниться
3. Проверьте консоль браузера (F12) на ошибки
4. Если есть ошибки - проверьте Network tab

## Отладка

Если все еще не работает:

1. Проверьте что сервер запущен:
   ```bash
   ps aux | grep vite
   ```

2. Проверьте логи:
   ```bash
   tail -f /tmp/vite_server.log
   ```

3. Проверьте что login.php возвращает JSON:
   ```bash
   curl -X POST http://localhost/planrun/login.php \
     -H "X-Requested-With: XMLHttpRequest" \
     -H "Accept: application/json" \
     -d "username=test&password=test"
   ```

4. Проверьте CORS заголовки в браузере (Network tab)
