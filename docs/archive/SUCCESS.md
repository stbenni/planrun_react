# ✅ УСПЕХ! React приложение запущено!

## Что сделано

1. ✅ Установлен nvm (Node Version Manager)
2. ✅ Установлен Node.js 20.20.0
3. ✅ Установлены все зависимости (280 пакетов)
4. ✅ Vite установлен и работает
5. ✅ Dev сервер запущен

## 🚀 Как зайти

### Вариант 1: Локально на сервере
```
http://localhost:3000
```

### Вариант 2: Через SSH туннель (с вашего компьютера)
```bash
ssh -L 3000:localhost:3000 st_benni@planrun.ru
```
Затем откройте в браузере: `http://localhost:3000`

### Вариант 3: Прямой доступ (если открыт порт 3000)
```
http://planrun.ru:3000
```

## 📝 Команды для управления

### Остановить сервер
```bash
pkill -f "vite"
```

### Перезапустить сервер
```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm use 20
cd /var/www/planrun/react/web
npm run dev
```

### Проверить статус
```bash
ps aux | grep vite
curl http://localhost:3000
```

## ⚙️ Важно

- **Node.js 20** используется через nvm
- При каждом новом терминале нужно загружать nvm:
  ```bash
  export NVM_DIR="$HOME/.nvm"
  [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
  nvm use 20
  ```

- Или добавить в `~/.bashrc` (уже добавлено nvm при установке)

## 🎉 Готово!

Приложение работает на порту 3000!
