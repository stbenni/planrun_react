# Документация: Профиль пользователя

## 📋 Обзор

Профиль пользователя в PlanRun содержит всю информацию, необходимую для создания персонализированного плана тренировок. Данные разделены на несколько вкладок для удобства навигации.

## 🗂️ Структура профиля

### Вкладка "👤 Профиль" (Личная информация)

#### Основные поля:
- **Аватар** - изображение профиля (JPEG, PNG, GIF, WebP, макс. 5MB)
- **Имя пользователя** * (обязательное, 3-50 символов)
- **Email** (опционально, валидация формата)
- **Пол** - Мужской / Женский / Не указано
- **Год рождения** (1900 - текущий год)
- **Рост (см)** (50-250 см)
- **Вес (кг)** (20-300 кг, с точностью до 0.1)
- **Часовой пояс** - выбор из списка популярных часовых поясов

---

### Вкладка "🏃 Тренировки" (объединяет Цели, Тренировки и Здоровье)

#### Секция "🎯 Мои цели"

**Тип цели** * (обязательное поле):
- `health` - Здоровье
- `race` - Забег
- `weight_loss` - Похудение
- `time_improvement` - Улучшение времени

##### Условные поля в зависимости от типа цели:

**Для цели "Забег" (`race`):**
- **Целевая дистанция забега** - 5 км / 10 км / Полумарафон (21.1 км) / Марафон (42.2 км)
- **Дата забега** (формат: YYYY-MM-DD)
- **Целевое время** (формат: HH:MM)

**Для цели "Улучшение времени" (`time_improvement`):**
- **Дата целевого марафона** (формат: YYYY-MM-DD)
- **Целевое время марафона** (формат: HH:MM)

**Для цели "Похудение" (`weight_loss`):**
- **Целевой вес (кг)** (20-300 кг, с точностью до 0.1)
- **Дата достижения цели** (формат: YYYY-MM-DD)

**Для цели "Здоровье" (`health`):**
- **Программа здоровья** - Начать бегать / Couch to 5K / Регулярный бег / Своя программа
- **Длительность программы (недели)** (1-52 недели) - показывается только если выбрана программа

---

#### Секция "🏃 Настройки тренировок"

**Уровень подготовки** * (обязательное поле):
- `novice` - Новичок (не бегаю или менее 3 месяцев)
- `beginner` - Начинающий (3-6 месяцев регулярного бега)
- `intermediate` - Средний (6-12 месяцев регулярного бега)
- `advanced` - Продвинутый (1-2 года регулярного бега)
- `expert` - Опытный (более 2 лет регулярного бега)

**Еженедельный объем (км)** (0-200 км, с точностью до 0.1)
- Пример: 20, 30, 40 км в неделю

**Тренировок в неделю** (автоматически вычисляется)
- Read-only поле, рассчитывается из количества выбранных дней для бега
- Обновляется автоматически при изменении "Предпочитаемые дни для бега"

**Предпочитаемые дни для бега** (чекбоксы)
- Пн / Вт / Ср / Чт / Пт / Сб / Вс
- Можно выбрать несколько дней
- Значения сохраняются как массив: `['mon', 'tue', 'wed', ...]`

**Предпочитаемые дни для ОФП** (чекбоксы)
- Пн / Вт / Ср / Чт / Пт / Сб / Вс
- Можно выбрать несколько дней
- Значения сохраняются как массив: `['mon', 'tue', 'wed', ...]`

**Предпочтения по ОФП**:
- `gym` - Тренажерный зал
- `home` - Дома
- `both` - И зал, и дома
- `group_classes` - Групповые занятия
- `online` - Онлайн

**Предпочтительное время тренировок**:
- `morning` - Утро
- `day` - День
- `evening` - Вечер

**Есть беговая дорожка** (чекбокс)
- Булево значение: `true` / `false`

**Режим тренировок**:
- `ai` - AI план
- `coach` - С тренером
- `both` - AI + тренер
- `self` - Самостоятельно

**Дата начала тренировок** (формат: YYYY-MM-DD)
- С какой даты пользователь планирует начать тренировки

---

#### Секция "💪 Здоровье и опыт"

**Заметки о здоровье** (текстовое поле, многострочное)
- Особенности здоровья, травмы, ограничения
- Максимальная длина не ограничена

**Комфортный темп** (формат: MM:SS)
- Ввод в формате минуты:секунды (например: `7:00` означает 7 минут на километр)
- Автоматически конвертируется в секунды для сохранения в БД
- Диапазон: от 3:00 (180 сек) до 10:00 (600 сек) на километр

**Для цели "Забег" (`race`):**

**Первый забег на эту дистанцию** (чекбокс)
- Булево значение: `true` / `false`

**Дистанция последнего забега**:
- `5k` - 5 км
- `10k` - 10 км
- `half` - Полумарафон
- `marathon` - Марафон
- `other` - Другая

**Дистанция последнего забега (км)** (показывается только если выбрано "Другая")
- Числовое поле (0-200 км, с точностью до 0.1)

**Время последнего забега** (формат: HH:MM)
- Показывается только для цели "Забег"

**Дата последнего забега** (формат: YYYY-MM-DD)
- Показывается только для цели "Забег"

---

### Вкладка "👥 Социальное" (Конфиденциальность)

**Уровень приватности**:
- `public` - Публичный (профиль и календарь видны всем)
- `private` - Приватный (только вы видите свой профиль)
- `link` - По ссылке (доступ только по специальной ссылке)

---

### Вкладка "🔗 Интеграции"

**Telegram бот**:
- Статус подключения (подключен / не подключен)
- ID Telegram аккаунта (если подключен)
- Кнопка отвязки Telegram

**Тип спортивного устройства**:
- Текстовое поле для указания типа устройства
- Примеры: Garmin, Apple Watch, Polar и т.д.
- Используется для синхронизации данных тренировок

---

### Вкладка "⚙️ Аккаунт"

**Выход из аккаунта**:
- Кнопка для выхода из системы

---

## 🔄 Логика работы

### Автоматические вычисления:

1. **`sessions_per_week`** автоматически вычисляется из `preferred_days.length`
   - При изменении чекбоксов дней для бега поле обновляется автоматически
   - Поле только для чтения

2. **`easy_pace_sec`** вычисляется из `easy_pace_min`
   - Формат ввода: `MM:SS` (например: `7:00`)
   - Конвертация: `minutes * 60 + seconds`
   - Сохраняется в БД как число секунд

### Условное отображение:

1. **Поля программы здоровья** показываются только если `goal_type === 'health'`
   - `health_program`
   - `health_plan_weeks` (показывается только если выбрана программа)

2. **Поля последнего забега** показываются только если `goal_type === 'race'`
   - `is_first_race_at_distance`
   - `last_race_distance`
   - `last_race_distance_km` (показывается только если `last_race_distance === 'other'`)
   - `last_race_time`
   - `last_race_date`

3. **Поля целевого забега** показываются только если `goal_type === 'race'`
   - `race_distance`
   - `race_date`
   - `race_target_time`

4. **Поля целевого марафона** показываются только если `goal_type === 'time_improvement'`
   - `target_marathon_date`
   - `target_marathon_time`

5. **Поля цели по весу** показываются только если `goal_type === 'weight_loss'`
   - `weight_goal_kg`
   - `weight_goal_date`

---

## 📊 Структура данных

### Формат данных в БД:

```sql
-- Основные поля профиля
username VARCHAR(50) NOT NULL
email VARCHAR(255)
gender ENUM('male', 'female') NULL
birth_year INT NULL
height_cm INT NULL
weight_kg DECIMAL(5,2) NULL
timezone VARCHAR(50) DEFAULT 'Europe/Moscow'

-- Цели
goal_type ENUM('health', 'race', 'weight_loss', 'time_improvement') DEFAULT 'health'
race_distance VARCHAR(20) NULL
race_date DATE NULL
race_target_time TIME NULL
target_marathon_date DATE NULL
target_marathon_time TIME NULL
weight_goal_kg DECIMAL(5,2) NULL
weight_goal_date DATE NULL

-- Тренировки
experience_level ENUM('novice', 'beginner', 'intermediate', 'advanced', 'expert') DEFAULT 'novice'
weekly_base_km DECIMAL(5,1) NULL
sessions_per_week INT NULL
preferred_days JSON NULL  -- массив: ['mon', 'tue', 'wed', ...]
preferred_ofp_days JSON NULL  -- массив: ['mon', 'tue', ...]
has_treadmill TINYINT(1) DEFAULT 0
training_time_pref ENUM('morning', 'day', 'evening') NULL
ofp_preference ENUM('gym', 'home', 'both', 'group_classes', 'online') NULL
training_mode ENUM('ai', 'coach', 'both', 'self') DEFAULT 'ai'
training_start_date DATE NULL

-- Здоровье
health_notes TEXT NULL
device_type VARCHAR(100) NULL
health_program ENUM('start_running', 'couch_to_5k', 'regular_running', 'custom') NULL
health_plan_weeks INT NULL
easy_pace_sec INT NULL  -- секунды на километр (180-600)
is_first_race_at_distance TINYINT(1) DEFAULT 0
last_race_distance VARCHAR(20) NULL
last_race_distance_km DECIMAL(5,1) NULL
last_race_time TIME NULL
last_race_date DATE NULL

-- Аватар и приватность
avatar_path VARCHAR(255) NULL
privacy_level ENUM('public', 'private', 'link') DEFAULT 'public'

-- Интеграции
telegram_id BIGINT NULL
```

### Формат данных в React (formData):

```javascript
{
  // Профиль
  username: string,
  email: string,
  gender: string | null,
  birth_year: string,
  height_cm: string,
  weight_kg: string,
  timezone: string,
  
  // Цель
  goal_type: 'health' | 'race' | 'weight_loss' | 'time_improvement',
  race_distance: string | null,
  race_date: string | null,
  race_target_time: string | null,
  target_marathon_date: string | null,
  target_marathon_time: string | null,
  weight_goal_kg: string | null,
  weight_goal_date: string | null,
  
  // Тренировки
  experience_level: 'novice' | 'beginner' | 'intermediate' | 'advanced' | 'expert',
  weekly_base_km: string,
  sessions_per_week: string,  // вычисляется автоматически
  preferred_days: string[],  // ['mon', 'tue', 'wed', ...]
  preferred_ofp_days: string[],  // ['mon', 'tue', ...]
  has_treadmill: boolean,
  training_time_pref: string | null,
  ofp_preference: string | null,
  training_mode: 'ai' | 'coach' | 'both' | 'self',
  training_start_date: string | null,
  
  // Здоровье
  health_notes: string,
  device_type: string,
  health_program: string | null,
  health_plan_weeks: string,
  easy_pace_min: string,  // формат MM:SS для отображения
  easy_pace_sec: string,  // секунды для сохранения в БД
  is_first_race_at_distance: boolean,
  last_race_distance: string | null,
  last_race_distance_km: string,
  last_race_time: string | null,
  last_race_date: string | null,
  
  // Аватар и приватность
  avatar_path: string,
  privacy_level: 'public' | 'private' | 'link',
  
  // Интеграции
  telegram_id: string,
}
```

---

## 🔧 API Endpoints

### GET `/api_wrapper.php?action=get_profile`
Получение данных профиля пользователя.

**Ответ:**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "username": "user123",
    "email": "user@example.com",
    "gender": "male",
    "birth_year": 1990,
    "height_cm": 175,
    "weight_kg": 70.5,
    "timezone": "Europe/Moscow",
    "goal_type": "race",
    "race_distance": "marathon",
    "race_date": "2026-05-03",
    "race_target_time": "03:30:00",
    "experience_level": "intermediate",
    "weekly_base_km": "40.0",
    "sessions_per_week": 4,
    "preferred_days": {"run": ["tue", "wed", "fri", "sun"], "ofp": ["tue", "thu", "sat"]},
    "preferred_ofp_days": ["tue", "thu", "sat"],
    "has_treadmill": 0,
    "training_time_pref": "morning",
    "ofp_preference": "gym",
    "training_mode": "ai",
    "training_start_date": "2025-11-24",
    "health_notes": "",
    "device_type": "Garmin",
    "health_program": null,
    "health_plan_weeks": null,
    "easy_pace_sec": 420,
    "is_first_race_at_distance": 0,
    "last_race_distance": null,
    "last_race_distance_km": null,
    "last_race_time": null,
    "last_race_date": null,
    "avatar_path": "/uploads/avatars/avatar_1_1234567890.jpg",
    "privacy_level": "public",
    "telegram_id": 123456789
  }
}
```

### POST `/api_wrapper.php?action=update_profile`
Обновление данных профиля.

**Тело запроса (JSON):**
```json
{
  "csrf_token": "token_here",
  "username": "user123",
  "email": "user@example.com",
  "gender": "male",
  "birth_year": "1990",
  "height_cm": "175",
  "weight_kg": "70.5",
  "timezone": "Europe/Moscow",
  "goal_type": "race",
  "race_distance": "marathon",
  "race_date": "2026-05-03",
  "race_target_time": "03:30",
  "experience_level": "intermediate",
  "weekly_base_km": "40.0",
  "sessions_per_week": "4",
  "preferred_days": ["tue", "wed", "fri", "sun"],
  "preferred_ofp_days": ["tue", "thu", "sat"],
  "has_treadmill": false,
  "training_time_pref": "morning",
  "ofp_preference": "gym",
  "training_mode": "ai",
  "training_start_date": "2025-11-24",
  "health_notes": "",
  "device_type": "Garmin",
  "health_program": null,
  "health_plan_weeks": null,
  "easy_pace_sec": "420",
  "is_first_race_at_distance": false,
  "last_race_distance": null,
  "last_race_distance_km": null,
  "last_race_time": null,
  "last_race_date": null,
  "avatar_path": "/uploads/avatars/avatar_1_1234567890.jpg",
  "privacy_level": "public"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Профиль успешно обновлен"
}
```

### POST `/api_wrapper.php?action=upload_avatar`
Загрузка аватара.

**Тело запроса (FormData):**
- `csrf_token`: string
- `avatar`: File (изображение)

**Ответ:**
```json
{
  "success": true,
  "data": {
    "avatar_path": "/uploads/avatars/avatar_1_1234567890.jpg"
  }
}
```

### POST `/api_wrapper.php?action=remove_avatar`
Удаление аватара.

**Тело запроса (JSON):**
```json
{
  "csrf_token": "token_here"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Аватар успешно удален"
}
```

### POST `/api_wrapper.php?action=update_privacy`
Обновление уровня приватности.

**Тело запроса (JSON):**
```json
{
  "csrf_token": "token_here",
  "privacy_level": "private"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Настройки приватности обновлены"
}
```

### POST `/api_wrapper.php?action=unlink_telegram`
Отвязка Telegram аккаунта.

**Тело запроса (JSON):**
```json
{
  "csrf_token": "token_here"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Telegram успешно отвязан"
}
```

---

## 🎨 Валидация

### Правила валидации:

1. **username**: 3-50 символов, обязательное поле
2. **email**: валидный формат email (если указан)
3. **birth_year**: 1900 - текущий год
4. **height_cm**: 50-250 см
5. **weight_kg**: 20-300 кг
6. **weekly_base_km**: 0-200 км
7. **sessions_per_week**: 1-7 тренировок
8. **easy_pace_sec**: 180-600 секунд (3:00 - 10:00 на км)
9. **health_plan_weeks**: 1-52 недели
10. **last_race_distance_km**: 0-200 км (если выбрано "Другая")

### Нормализация данных:

- Пустые строки (`''`) и `null` конвертируются в `null` для сохранения в БД
- Массивы `preferred_days` и `preferred_ofp_days` сохраняются как JSON
- Булевы значения (`true`/`false`) конвертируются в `1`/`0` для БД
- Темп в формате `MM:SS` конвертируется в секунды для сохранения

---

## 🔄 Миграция данных

### Объединение полей опыта:

Старые поля `current_running_level` и `running_experience` были объединены в одно поле `experience_level` с новыми значениями:

**Маппинг старых значений:**
- Старое `experience_level: 'beginner'` → Новое `experience_level: 'beginner'`
- Старое `experience_level: 'intermediate'` → Новое `experience_level: 'intermediate'`
- Старое `experience_level: 'advanced'` → Новое `experience_level: 'advanced'`

**Новые значения:**
- `novice` - для новых пользователей или тех, кто не бегает
- `expert` - для очень опытных бегунов (более 2 лет)

---

## 📝 Примечания для разработчиков

1. **Контролируемые компоненты**: Все поля формы должны быть инициализированы как строки (`''`) или массивы (`[]`), а не `null`, для правильной работы React контролируемых компонентов.

2. **Автоматическое обновление**: Поле `sessions_per_week` обновляется автоматически при изменении `preferred_days` через функцию `toggleDay()`.

3. **Условное отображение**: Используйте условный рендеринг (`{condition && <Component />}`) для показа полей в зависимости от `goal_type`.

4. **Формат темпа**: Пользователь вводит темп в формате `MM:SS`, но в БД сохраняется как секунды. Конвертация происходит в `onChange` обработчике.

5. **Массивы дней**: Значения дней недели используют сокращенные названия: `'mon'`, `'tue'`, `'wed'`, `'thu'`, `'fri'`, `'sat'`, `'sun'`.

6. **Обратная совместимость**: Бэкенд поддерживает старые значения `experience_level` (`beginner`, `intermediate`, `advanced`) для обратной совместимости.

---

## 🐛 Известные проблемы и решения

### Проблема: Select элементы не отображают выбранное значение
**Решение**: Используйте `key` prop с динамическим значением для принудительного ре-рендера:
```jsx
<select key={`field-${formData.field || 'empty'}`} value={formData.field || ''}>
```

### Проблема: Массивы дней не отображаются как выбранные
**Решение**: Используйте `Array.isArray()` проверку в `checked` атрибуте:
```jsx
checked={Array.isArray(formData.preferred_days) && formData.preferred_days.includes(day.value)}
```

### Проблема: Темп не сохраняется правильно
**Решение**: Убедитесь, что конвертация `MM:SS` → секунды происходит перед отправкой на сервер.

---

## 📚 Дополнительные ресурсы

- [Архитектура проекта](./ARCHITECTURE_ANALYSIS_2026.md)
- [Анализ формы профиля](./FORM_ANALYSIS.md)
- [API документация](../planrun-backend/README.md)
